Extract architectural entities referenced in each numbered code repo knowledge item below. Return only entities explicitly mentioned or clearly implied. Types: module (code packages, services, lambdas), data_entity (domain objects like Order, Payment), concept (business concepts like reconciliation, return flow), pattern (architectural patterns in use). Only extract proper nouns in the project context — specific module names, domain objects, named patterns. Do not extract generic programming terms like "function", "class", "service", "database", "API".