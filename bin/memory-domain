#!/bin/sh
set -e

SCRIPT_DIR="$(cd "$(dirname "$0")/.." && pwd)"
CLI_ENTRY="$SCRIPT_DIR/src/cli/cli.ts"

if command -v bun >/dev/null 2>&1; then
  exec bun run "$CLI_ENTRY" "$@"
else
  echo "Error: bun is required. Install from https://bun.sh" >&2
  exit 1
fi
